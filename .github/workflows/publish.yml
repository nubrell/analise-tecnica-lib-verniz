name: Publish Packages

on:
  push:
    tags:
      - 'v*'  # Publica quando uma tag versão é criada (ex: v1.0.0)
  workflow_dispatch: # Permite disparar manualmente

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Build and Publish Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@nubrell'

      - name: Install Dependencies
        working-directory: lib-verniz-starter
        run: |
          # Instala todas as dependências incluindo devDependencies
          yarn install --frozen-lockfile --production=false
          
          # Verifica se os pacotes Radix foram instalados
          echo "Checking Radix UI packages..."
          ls -la node_modules/@radix-ui/ || echo "Radix packages not found in root"
          
          # Garante que os pacotes estão disponíveis
          yarn install --check-files

      - name: Build @nubrell Packages
        working-directory: lib-verniz-starter
        run: |
          echo "Building @nubrell packages..."
          
          # Adiciona node_modules/.bin ao PATH
          export PATH="$PWD/node_modules/.bin:$PATH"
          
          # Função para fazer build de um pacote
          build_package() {
            local package_dir=$1
            local package_name=$(grep -o '"name": "[^"]*"' "$package_dir/package.json" | cut -d'"' -f4)
            
            if [[ "$package_name" != @nubrell/* ]]; then
              return 0
            fi
            
            echo "Building $package_name..."
            cd "$package_dir"
            
            # Garante que as dependências estão instaladas neste workspace
            # O yarn workspaces pode não ter instalado as devDependencies aqui
            if [ -f "package.json" ]; then
              echo "Installing dependencies for $package_name..."
              # Cria um link simbólico para node_modules da raiz se não existir
              if [ ! -d "node_modules" ]; then
                ln -s ../../node_modules node_modules || true
              fi
              # Instala dependências específicas deste pacote se necessário
              yarn install --check-files || true
            fi
            
            # Faz o build
            yarn build || npx tsup
            
            cd - > /dev/null
          }
          
          # Build utils
          if [ -d "packages/utils" ] && [ -f "packages/utils/package.json" ]; then
            build_package "packages/utils"
          fi
          
          # Build todos os componentes
          for dir in packages/components/*/; do
            if [ -d "$dir" ] && [ -f "$dir/package.json" ]; then
              build_package "$dir"
            fi
          done
          
          echo "✅ All packages built!"

      - name: Publish to GitHub Packages
        working-directory: lib-verniz-starter
        run: |
          echo "Publishing @nubrell packages to GitHub Packages..."
          
          # Função para verificar e publicar um package
          publish_package() {
            local package_dir=$1
            local package_name=$(grep -o '"name": "[^"]*"' "$package_dir/package.json" | cut -d'"' -f4)
            
            # Só publica se começar com @nubrell
            if [[ "$package_name" != @nubrell/* ]]; then
              echo "⏭️  Skipping $package_name (not a @nubrell package)"
              return 0
            fi
            
            # Verifica se tem build gerado
            if [ ! -f "$package_dir/dist/index.cjs" ] && [ ! -f "$package_dir/dist/index.mjs" ]; then
              echo "⏭️  Skipping $package_name (no build files found)"
              return 0
            fi
            
            echo "📦 Publishing $package_name..."
            cd "$package_dir"
            
            # Tenta publicar
            if npm publish --access restricted; then
              echo "✅ Successfully published $package_name"
            else
              exit_code=$?
              if [ $exit_code -eq 1 ]; then
                echo "⚠️  $package_name already published or version exists (continuing...)"
              else
                echo "❌ Failed to publish $package_name"
                exit $exit_code
              fi
            fi
            cd - > /dev/null
          }
          
          # Publicar utils primeiro (se for @nubrell/utils)
          if [ -d "packages/utils" ] && [ -f "packages/utils/package.json" ]; then
            publish_package "packages/utils"
          fi
          
          # Publicar todos os componentes
          for dir in packages/components/*/; do
            if [ -d "$dir" ] && [ -f "$dir/package.json" ]; then
              publish_package "$dir"
            fi
          done
          
          echo "✨ Finished publishing packages!"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
