---
to: packages/components/<%= h.changeCase.paramCase(name) %>/src/types.ts
---
// Add shared types for <%= h.changeCase.pascalCase(name) %> component here

export interface <%= h.changeCase.pascalCase(name) %>BaseProps {
  // Add base props here
}

